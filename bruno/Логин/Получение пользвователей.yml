info:
  name: Получение пользвователей
  type: http
  seq: 12

http:
  method: GET
  url: "{{u}}/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
